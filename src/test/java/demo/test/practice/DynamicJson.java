package demo.test.practice;

import org.junit.jupiter.api.Test;

//import static org.hamcrest.Matcher.*;
public class DynamicJson {


    @Test
    public void addBook() {

        //  get("/products")
        // .then().assertThat()
        // .body(matchesJsonSchemaInClasspath("products-schema.json"));

        /*// Given
        JsonSchemaFactory jsonSchemaFactory = JsonSchemaFactory.newBuilder().setValidationConfiguration(ValidationConfiguration
                .newBuilder().setDefaultVersion(DRAFTV4).freeze()).freeze();

// When
        get("/products").then().assertThat().body(matchesJsonSchemaInClasspath("products-schema.json").using(jsonSchemaFactory));
*/


    }
}
